Instance Fields
   - The variables which are declared
   with in the method , with self as
   the first argument.

   x Instance methods can perform the operations on
     both static and non static fields [ Instance fields ]

   - Every instance fields must be referred self
     throughout the same class.

   - every  instance field must be referred an
   object reference whenever u want access it from
   outside of the class.

   - For instance fields memory is allocated with in
   the object

   - For every instance field an individual memory blocks
   are created in every new object of the same class
   thus changing the value of instance field in one
   object which is not effected to the same instance field
   of another object of the same type.



   



   



   




    

   

   
